package pe.edu.utp.tiendazapatillas.repositorio.interfaces;

// Aún no tenemos la entidad Venta, pero podemos definir la interfaz.
// Por ahora, la dejaremos vacía hasta tener el modelo de Venta.
// Esto demuestra la flexibilidad de depender de abstracciones.
public interface IRepositorioVenta {
    // Métodos como guardarVenta(Venta venta), buscarVenta(int id), etc. se añadirán aquí.
}
